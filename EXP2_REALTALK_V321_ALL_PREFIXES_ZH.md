# V3.2.1 十人物等量连续前缀验证

固定十位人物各Cb开头连续20条，共200条；不按分数、困难标签或输出挑选。
复用Emi/Nicolas/Kevin已经完成的60条输出和全部八项评分，只新增其余七人140条。
Vanessa前20条与以前困难60条不重叠；困难窗口不并入本轮主汇总。
Vanessa的新前缀使用同一冻结配置重新建立Self Domain，不声称复用旧画像随机实现。

生成代码、Prompt、Schema、模型、解码、重试和完整因果上下文均冻结于4302989；
启动前比较模型/协议/Prompt/Schema/实现/解码哈希。没有新语义规则或模型更换。
deepseek-v4-flash不思考；Judge沿用现有gpt-4o-mini及论文Prompt，CPU-only。

逐人生成20条并进行完整八项评价，所有人同一预定规模，不根据中间分数决定是否保留。
某人失败则记录为stopped并保留检查点；继续其他人的独立任务，不无限重试。
任何一人不完整都不生成完整200条主汇总，不能把该人从平均值中删除。
已有三人的原始产物与评分逐行复用，不重新Judge。新人物共享canonical V9参考标签。

主汇总为speaker macro mean，保存每人20条及逐样本结果、来源文件SHA256。
200条均为早期前缀，不代表所有三个Session，也不是全519条的论文主结果。
预先存在的519条多轮诊断意味着本轮仅为探索性泛化检查，而非独立确认性复现。
论文逐列最佳仅作参考；在结果齐全前不宣布优胜，不修改当前Prompt。

入口 tools/run_realtalk_v321_all_prefixes.sh OUTPUT_ROOT。
执行器为兼容冻结模块使用gate=60，实际通过明确selected_ids_file给每个任务20条；
manifest实际completed_records、输入ID及目录名均按20条记录，不称其为60条新任务。
完成即停，不自动扩到519条。
